<!DOCTYPE html>
<html>
<head>
<style type="text/css">
	table, th, td{
		border: 1px solid black;
		text-align: center;
		padding: 3px 10px;
	}
</style>
	<title>Shoes</title>
</head>
<body>
 | <a href="/users/<%=current_user.id %>">Go to Dashboard</a>
<h2>Hi <%= current_user.first %></h2>

	<h3>All Products</h3>
	<table>
		<tr>
			<th>Product</th>
			<th>Seller</th>
			<th>Date Posted</th>
			<th>Amount</th>
			<th>Action</th>
		</tr>
		<% @shoes.each do |shoe| %>
		<tr>
			<td><%= shoe.product %></td>
			<td><%= shoe.seller.first %> <%= shoe.seller.last %></td>
			<td><%= shoe.created_at.strftime("%B %d, %Y") %></td>
			<td><%= shoe.amount %></td>
			<td><a href="/shoes/<%= shoe.id %>" data-method='patch'>Buy</a></td>
		</tr>
		<% end %>
	</table>
	

</body>
</html>